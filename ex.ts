function multiplica(a: number, b: number): number {
    return a * b;
}

const multiplicador = multiplica(10,2);

console.log('Resultado do calculo:', multiplicador);


function hello(nome: string): string {
return nome;
}

const hello_people = hello('Ebac');

console.log('Hello', hello_people);